version: '3.8'

services:
  app:
    build: .
    ports:
      - "3007:3007"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - app